@model UserInfo
@{
    ViewData["Title"] = "Başvuru Detayı";
}

@{
    var className = @Model.WillAttend == true ? "success":"danger";
}
@{
    var attend = @Model.WillAttend == true ? "Katılıyor":"Katılmıyor";
}

<main class="text-center">
    <h1 class="h4">Başvuru Detayı</h1>
    <span class="badge bg-@className">
        @attend
    </span>
    <p>İsim : @Model.Name</p>
    <p>Email : @Model.Email</p>
    <p>Telefon : @Model.Phone</p>
    <a asp-controller="Meeting" asp-action = "List" class="btn btn-outline-@className">Katılımcı Listesi</a>

</main>